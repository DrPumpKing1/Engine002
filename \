#pragma once

#include <glm/glm.hpp>

#include "../Mesh.h"

class Primitive {
    private:
        Mesh mesh;
    public:
        const Mesh &GetMesh() const { return mesh; }
    private:
        virtual void ConstructMesh();
};
